[package]
name = "lau-voice"
version = "0.1.0"
edition = "2021"
description = "Voice interface for the Lau platform — TTS/STT abstractions for kids talking to their game world"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
